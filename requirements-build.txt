# Build Requirements for Tick-Tock Widget
# Install with: pip install -r requirements-build.txt
# These are the minimal dependencies needed to build the executable

# PyInstaller for creating executables
pyinstaller>=6.0.0

# Build system requirements
setuptools>=68.0.0
wheel>=0.41.0
build>=0.10.0
